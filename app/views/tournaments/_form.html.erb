<%= form_for(@tournament) do |f| %>
    <% if @tournament.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@tournament.errors.count, "error") %> prohibited this tournament from being saved:</h2>

          <ul>
            <% @tournament.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, :class => "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :tournament_type %>
      <%= f.select :tournament_type, options_for_select(Tournament::TYPES), {}, :class => "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :number_of_players %>
      <%= f.number_field :number_of_players, :class => "form-control" %>
    </div>

    <%= f.fields_for :players do |p| %>
        <div class="form-group">
          <%= p.label :name %>
          <%= p.text_field :name, :class => "form-control" %>
        </div>
        <div class="form-group">
          <%= p.label :team %>
          <%= p.text_field :team, :class => "form-control" %>
        </div>
    <% end %>
    <div class="actions">
      <%= f.submit "Save", :class => "btn btn-default" %>
    </div>
<% end %>
